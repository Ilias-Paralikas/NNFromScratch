These files are the specifics of trained networks
their names describe the number of neurons per layer with x's between them
W (*number*) is the initial Weight Range
B (*Number*) is the initial Bias Range we gave 
And Acc is accuracy